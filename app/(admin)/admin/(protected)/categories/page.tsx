import { AdminPlaceholderPage } from "@/components/layout/admin-placeholder-page";

export default function AdminCategoriesPage() {
  return (
    <AdminPlaceholderPage
      eyebrow="Categories"
      title="Hierarchy-aware taxonomy management"
      description="This area will manage the multi-level category tree, localized slug paths, featured states, and category landing page content."
    />
  );
}
