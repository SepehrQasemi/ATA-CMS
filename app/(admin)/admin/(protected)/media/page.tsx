import { AdminPlaceholderPage } from "@/components/layout/admin-placeholder-page";

export default function AdminMediaPage() {
  return (
    <AdminPlaceholderPage
      eyebrow="Media"
      title="Shared image and document library"
      description="This area will support safe uploads, asset metadata, and product document attachments without introducing a heavyweight external DAM."
    />
  );
}
