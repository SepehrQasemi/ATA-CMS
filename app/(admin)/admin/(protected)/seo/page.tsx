import { AdminPlaceholderPage } from "@/components/layout/admin-placeholder-page";

export default function AdminSeoPage() {
  return (
    <AdminPlaceholderPage
      eyebrow="SEO"
      title="Metadata and publication diagnostics"
      description="This area will centralize metadata health, locale publication mismatches, sitemap visibility, and publication safety checks."
    />
  );
}
